# Development checks

Run `bun check` before integrating UI changes. Do not disable lint or type-check rules, add blanket suppression comments, or weaken compiler settings to make it pass; fix the underlying types and behavior instead.
