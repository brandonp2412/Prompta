{
  "name": "prompta-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/prompta/ui/app.ts --outfile src/prompta/static/app.js --target browser",
    "typecheck": "tsc --noEmit",
    "test:ui": "bun test src/prompta/ui",
    "check": "bun run typecheck && bun run test:ui && bun run build"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
